# Cursor Rules Index
# Generated by Forgewright v8.1

> This is the master index of all Cursor rules for this project.
> Add new rules to `.cursor/rules/` directory and update this file.

---

## Overview

This project uses Cursor rules to provide project-specific guidance to the AI.
Rules are stored in `.cursor/rules/` and loaded based on file patterns.

---

## Available Rules

| Rule | File | Scope | Purpose |
|------|------|-------|---------|
{{#each rules}}
| **{{this.name}}** | `{{this.file}}` | `{{this.scope}}` | {{this.purpose}} |
{{/each}}

---

## Quick Reference

### By Scope

#### TypeScript/JavaScript
```
*.ts *.tsx *.js *.jsx
```

#### Configuration
```
*.json *.yaml *.yml *.toml
```

#### Documentation
```
*.md *.mdx *.rst
```

#### Testing
```
*.test.ts *.spec.ts __tests__/** *.e2e.ts
```

---

## Adding a New Rule

1. Create a new `.md` file in `.cursor/rules/`
2. Follow the `rule.md.hbs` template structure
3. Add an entry to this index
4. Test the rule in Cursor

## Rule Priority

Rules are applied in order of specificity:
1. Exact file matches (highest priority)
2. Pattern matches (*.ts, src/**)
3. Directory matches
4. Global rules (lowest priority)

## Best Practices

- Keep rules concise (< 500 words)
- Use specific scopes
- Include concrete examples
- Avoid conflicting rules
- Review rules quarterly
